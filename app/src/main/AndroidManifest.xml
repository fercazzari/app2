<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pp2.app2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="Super En Casa"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ProductoActivity"
            android:label="Producto" />
        <activity
            android:name=".activities.CompraActivity"
            android:label="Compra" />
        <activity
            android:name=".activities.SugerenciaActivity"
            android:label="Sugerencia" />
        <activity
            android:name=".activities.CompraFinalizadaActivity"
            android:label="Compra Finalizada" />
        <activity
            android:name=".activities.DomicilioActivity"
            android:label="Domicilio" />
        <activity
            android:name=".activities.PagoActivity"
            android:label="Medio de Pago" />
        <activity android:name=".activities.TestActivity"></activity>
    </application>

</manifest>